'use client'


export default function DefaultAdmin() {


    return (
        <div className="flex w-full flex-col items-center min-h-screen py-10">
            {/* {userIsActive ? `${router.push('/detailsAdmin/home')}` : `${router.push('/login')}`} */}
        </div>
    )
}