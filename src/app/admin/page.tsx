'use client'


export default function DefaultAdmin() {


    return (
        <div className="flex w-full flex-col bg-store-secondary items-center min-h-screen py-10 overflow-hidden">
            {/* {userIsActive ? `${router.push('/admin/home')}` : `${router.push('/login')}`} */}
            test
        </div>
    )
}